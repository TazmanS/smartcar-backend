# SmartCar Backend

Backend service for an ESP32-based SmartCar.

## Run

Create `.env`:

```env
PORT=:8080
```

Start the server:

```bash
go run .
```

The API will be available at:

```text
http://localhost:PORT
```
